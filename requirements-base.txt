# Base requirements that must be installed first for Python 3.13 compatibility
# Install this before requirements.txt: pip install -r requirements-base.txt
# These versions ensure compatibility with Python 3.13 and packages that build from source
setuptools>=69.0.0
wheel>=0.42.0
pip>=24.0
